# Cross-symbol Transformer config (Stage 4)
num_symbols: 20
feature_dim: 32
num_heads: 8
num_layers: 6
seq_len: 128
dropout: 0.1
market_context: true

training:
  epochs: 30
  batch_size: 16
  learning_rate: 0.0001
  warmup_steps: 1000
