global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "flash-crash-detector"
    static_configs:
      - targets: ["detector:8000"]

  - job_name: "flash-crash-proxy"
    static_configs:
      - targets: ["proxy:8000"]
